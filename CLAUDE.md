# moment

## Formatting

Run `swiftformat .` after making any code changes.

## Concurrency

Use Swift 6 concurrency (async/await, `Sendable`, actors). Do not downgrade to Swift 5 concurrency mode.

## Before committing

Run `swift test` and ensure all tests pass before committing.
